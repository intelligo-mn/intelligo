import { AfterViewInit, OnInit } from '@angular/core';
import { MatDialog } from '@angular/material';
import { ActivatedRoute, Router } from '@angular/router';
import { BusinessAccount, User } from '../../../models/data.models';
import { DataService } from '../../../services/data.service';
import { InfoDialogService } from '../../../services/info-dialog.service';
import { AppHeaderBarComponent } from '../../shared/app-header-bar/app-header-bar.component';
export declare class UsersComponent implements OnInit, AfterViewInit {
    private route;
    private router;
    private infoDialog;
    private dialog;
    private dataService;
    ngAfterViewInit(): void;
    appHeader: AppHeaderBarComponent;
    businessAccount: BusinessAccount;
    bizId: string;
    constructor(route: ActivatedRoute, router: Router, infoDialog: InfoDialogService, dialog: MatDialog, dataService: DataService);
    ngOnInit(): void;
    search: string;
    searchStart(): void;
    loadBusinessDetails(): void;
    createUserDialog(): void;
    users: User[];
    page: number;
    totalPages: number;
    prevPage(): void;
    nextPage(): void;
    view(user: User): void;
    loadUsers(): void;
    updateUserPassword(user: User): void;
    userRole(user: User): string;
}
